#include<stdio.h>
#include<stdlib.h>
void insertion(int *a,int n)
{
int key,i,j;
  for(i=1;i<n;i++)
  {
    key=a[i];
    j=i-1;
    while(j>=0 && key < a[j])
    { 
      a[j+1]=a[j];
      j--;
    }
    a[j+1]= key;
  }
}
int main()

{
int i,n,a[100];
scanf("%d",&n);
for(i=0;i<n;i++)
{
  scanf("%d",&a[i]);
}
insertion(a,n);
for(i=0;i<n;i++)
{
printf("\n %d ",a[i]);
}
return 0;
}
